{% extends 'base.html.twig' %}

{% block stylesheets %}{{parent()}}{% endblock %}

{% block body %}
    <div class="breadcrumb-area">
        <div class="top-breadcrumb-area bg-img bg-overlay d-flex align-items-center justify-content-center"
             style="background-image: url({{ asset('img/bg-img/24.jpg') }});">
            <h2></h2>

        </div>
    </div>


    <section class="alazea-blog-area mb-100">
        <div class="container">
            <div class="row">
                <div class="col-12 col-md-8">
                    <div class="row">

                        <!-- Single Blog Post Area -->
                        <div class="col-12 col-lg-6">
                            <div class="single-blog-post mb-50">

                                <div class="col-12">
                                    <nav aria-label="breadcrumb">
                                        <ol class="breadcrumb">
                                            <li class="breadcrumb-item"><a href="{{ path('listeQuestion') }}"><i class="fa fa-home"></i> accueil de forum</a></li>
                                            <li class="breadcrumb-item"><a href="#">detail de question</a></li>

                                        </ol>
                                    </nav>
                                </div>

                                <br>
                                <br>
                                <br>
                                {% for ch in chaine %}
                              <h4>      {{ ch }}
                                {% endfor %}
                               n°:{{ id }} <br> </h4>
                                titre: {{ titre }} <br>
                                description: {{ description }} <br>

                                <div class="post-meta">
                                    <a href="#"><i class="fa fa-clock-o" aria-hidden="true"></i> {{ datequestion }}
                                    </a>
                                    <a href="#"><i class="fa fa-user" aria-hidden="true"></i> {{ username }}</a>
                                </div>



                                <div class="row-sm">
                                  <a href="{{ path('updatequestion',{ 'id': id }) }}">
                                      <button type="button" class="btn btn-success">modifier</button>
                                  </a>
                              <a href="{{ path('affichereponse',{ 'id': id }) }}">
                                    <button type="button" class="btn btn-success">liste des commentaire</button>
                                </a>
                                </div>
                            </div>
<br>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </section>




{% endblock %}

{% block javascripts %}{{parent()}}{% endblock %}